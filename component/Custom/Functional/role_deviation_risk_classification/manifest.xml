<component>
  <description>
	Provides a plugable interface and framework to calculate the risk
    of a given object either by the class name or the overridden label.
    Will utilize hid_risk_logic_mapping table in extdb to determine which
    components will be used for each action.
    
    The RiskScoreClassificationPolicies will then be used to map ranges
    of risk scores to static labels for easier usage.
  </description>

  <version>1.0</version>
  <removable>True</removable>
  <priority>1</priority>

  <depends>Functional.hid_extdb</depends>

  <extension type="include">
    <filename>model.py</filename>
  </extension>

</component>